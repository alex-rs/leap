import type { KnipConfig } from "knip";

// knip.config.ts — dead-code detection for %%PROJECT_NAME%%
//
// Generated by leap scaffolder.
// Run: npx knip
//
// Knip reports: unused files, unused exports, unused types, unused dependencies,
// unused devDependencies, unlisted dependencies, and duplicate exports.
//
// Customize the `entry` and `project` globs below to match your project layout.
// For monorepos, replace the single-project config with a `workspaces` map.
// See: https://knip.dev/reference/configuration

const config: KnipConfig = {
  // Entry points — files that are the roots of the dependency graph.
  // Knip traces imports from these files to find what is used vs. unused.
  entry: [
    "src/index.ts",
    "src/index.tsx",
    // Common framework entry points — remove what does not apply.
    "src/app/**/{page,layout,loading,error,not-found,route,template,default}.{ts,tsx}",
    "src/app/**/_actions/*.ts",
    "src/pages/**/*.{ts,tsx}",
    "src/main.{ts,tsx}",
    "*.config.{ts,js,mjs,cjs}",
  ],

  // Project files — all source files knip should be aware of.
  project: ["src/**/*.{ts,tsx}"],

  // Ignore patterns — paths knip should not report as unused.
  // Add generated directories, UI component libraries, etc.
  ignore: [
    "**/*.d.ts",
    "**/*.gen.ts",
  ],

  // Issue types to report — full dead-code detection.
  include: [
    "files",
    "dependencies",
    "devDependencies",
    "unlisted",
    "exports",
    "types",
    "duplicates",
  ],
};

export default config;
